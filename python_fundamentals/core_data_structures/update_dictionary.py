#!/usr/bin/env python3
"""Module that updates or adds a key/value pair in a dictionary."""


def update_dictionary(a_dictionary, key, value):
    """Set a_dictionary[key] to value and return the dictionary."""
    a_dictionary[key] = value
    return a_dictionary
